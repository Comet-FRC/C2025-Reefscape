/*
package frc.robot.subsystems.vision.algae;

import edu.wpi.first.math.geometry.Translation2d;
import frc.robot.subsystems.vision.algae.AlgaeVision.TrackedAlgae;

public class AlgaeVisionIOSim implements AlgaeVisionIO {
  @Override
  public void updateInputs(AlgaeVisionIOInputs inputs) {
    inputs.trackedAlgae =
        new TrackedAlgae[] {
          new TrackedAlgae(new Translation2d(1, 5), Math.PI),
          new TrackedAlgae(new Translation2d(5, 4), Math.E),
          new TrackedAlgae(new Translation2d(8, 3), 5),
        };
  }
}
  */
